🚨 FINAL RLS DISABLE - DO THIS NOW OR APP WILL NOT WORK

CURRENT ERROR:
"permission denied for schema public" - RLS is BLOCKING all database access

ROOT CAUSE:
Row Level Security (RLS) is ENABLED in Supabase on all tables

SOLUTION:
Disable RLS in Supabase Dashboard

⚠️ CRITICAL: You MUST do this in Supabase, not in your code

STEP-BY-STEP:

1. Open browser
2. Go to: https://app.supabase.com
3. Login if needed
4. Click on "braidly" project (select it)
5. Left sidebar → Click "SQL Editor"
6. Click "New Query" button
7. DELETE any existing text in the editor
8. Copy and paste EXACTLY these 10 lines (no comments, no extra text):

---COPY FROM HERE---
ALTER TABLE public.profiles DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.braider_profiles DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.services DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.portfolio_images DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.gallery_images DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.bookings DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.payments DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.reviews DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.verification_documents DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.notifications DISABLE ROW LEVEL SECURITY;
---COPY TO HERE---

9. Click the blue "Run" button
10. Wait for the query to complete
11. You should see "Query executed successfully" or similar message

VERIFY IT WORKED:

12. Click "New Query" again
13. Paste this verification query:

SELECT tablename FROM pg_tables WHERE schemaname='public' AND rowsecurity=true;

14. Click "Run"
15. Look at the results:
    - If EMPTY (0 rows) = SUCCESS ✅ RLS is disabled
    - If shows table names = FAILED ❌ RLS still enabled

IF VERIFICATION SHOWS SUCCESS:

16. Go back to your browser with the app
17. Hard refresh: Ctrl+Shift+R (Windows) or Cmd+Shift+R (Mac)
18. Clear browser cache: Ctrl+Shift+Delete
19. Close browser completely
20. Reopen browser
21. Go to: http://localhost:3003
22. Try login/signup again

EXPECTED RESULT AFTER RLS DISABLED:
✅ Login works without errors
✅ Signup works without errors
✅ Profiles are created automatically
✅ No more 403 Forbidden errors
✅ No more "permission denied" errors

IF IT STILL DOESN'T WORK:
- The database might be corrupted
- You may need to reset the entire Supabase project
- Contact Supabase support

DO NOT PROCEED WITH ANYTHING ELSE UNTIL RLS IS DISABLED IN SUPABASE
