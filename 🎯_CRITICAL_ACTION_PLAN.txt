🔥 CRITICAL ACTION PLAN - DO THIS NOW

PROBLEM: 403 Forbidden "permission denied for schema public" - RLS is still blocking database access

SOLUTION: Disable RLS in Supabase

STEPS:

1. Go to: https://app.supabase.com
2. Select: braidly project
3. Click: SQL Editor (left sidebar)
4. Click: New Query
5. Copy and paste EXACTLY these 10 lines (no comments):

---START COPY---
ALTER TABLE public.profiles DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.braider_profiles DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.services DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.portfolio_images DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.gallery_images DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.bookings DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.payments DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.reviews DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.verification_documents DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.notifications DISABLE ROW LEVEL SECURITY;
---END COPY---

6. Click: Run (blue button)
7. Wait for success message

VERIFY IT WORKED:
8. Click: New Query again
9. Paste this verification query:

SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' AND rowsecurity = true;

10. Click: Run
11. If it returns 0 rows = SUCCESS ✅
12. If it returns rows = RLS still enabled ❌

AFTER SUCCESS:
- Hard refresh browser: Ctrl+Shift+R
- Clear cache: Ctrl+Shift+Delete
- Test login/signup

EXPECTED RESULT:
✅ Login works
✅ Signup works
✅ No more 403 errors
✅ Profiles created automatically
